	public	__idivs
__idivs:
; I: UHL=dividend, UBC=divisor
; O: uhl=UHL/UBC

	push	bc
	push	de
	push	af

	ex	de, hl

	sbc	hl, hl
	ccf
	sbc	hl, bc
	inc	hl
	jp	m, .neg_divisor_skip
	cpl
	push	hl
	pop	bc
.neg_divisor_skip:

	sbc	hl, hl
	ccf
	sbc	hl, de
	inc	hl
	jp	p, .pos_dividend_skip
	cpl
	ex	de, hl
.pos_dividend_skip:

	push	af

	call	__idvrmu
	ex	de, hl

	pop	bc
	pop	af
	cp	a, b

	pop	de
	pop	bc
	ret	nz
	jp	__ineg


	extern	__idvrmu
	extern	__ineg
