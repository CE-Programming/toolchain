	assume	adl=1

	public	_imaxabs
_imaxabs:
	public	_llabs
_llabs:
	pop	iy
	pop	hl
	pop	de
	pop	bc
	push	bc
	push	de
	push	hl
	ld	a, b
	or	a, a
	call	m, __llneg.hijack_nc
	jp	(iy)


	extern	__llneg.hijack_nc
