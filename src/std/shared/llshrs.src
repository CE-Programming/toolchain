	public	__llshrs
__llshrs:
; Suboptimal
	push	af
	push	iy
	ld	iy, 0
	add	iy, sp
	ld	a, c
	ld	c, b
	ld	b, (iy + 9)
	inc	b
	dec	b
	jr	z, .finish
	push	de
	push	hl
.loop:
	sra	c
	rra
	rr	(iy - 1)
	rr	d
	rr	e
	rr	(iy - 4)
	rr	h
	rr	l
	djnz	.loop
	ld	sp, iy
.finish:
	ld	b, c
	ld	c, a
	pop	iy
	pop	af
	ret
