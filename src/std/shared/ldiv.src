	assume	adl=1

	public	_ldiv
_ldiv:

	pop	hl
	pop	iy
	ld	(iy), hl
	pop	hl
	pop	de
	pop	bc
	ex	(sp), hl
	ld	a, l
	ex	(sp), hl

	xor	a, e
	push	af
	xor	a, e

	push	de

	bit	7, e
	call	__ldivs_lrems_common

	call	__ldvrmu

	exx
	ld	e, a
	pop	af
	call	m, __lneg
	ld	(iy + 7), e
	ld	(iy + 4), hl

	exx
	pop	af
	call	m, __lneg
	ld	(iy + 3), e
	ld	de, (iy)
	ld	(iy), hl

	push	de
	push	de
	push	de
	push	de
	push	de
	bit	2, c
	ret	z
	ei
	ret


	extern	__ldivs_lrems_common
	extern	__ldvrmu
	extern	__lneg
