	assume	adl=1

	section	.text
	public	__bdvrms
__bdvrms:
; I: C=divisor, L=dividend
; O: a=?, h=abs(L)%abs(C), l=abs(L)/abs(C)

	ld	a, l
	public	__bdvrms.hijack_a_l_dividend
.hijack_a_l_dividend:
	rla
	jr	nc, .neg_divisor_skip
	xor	a, a
	sub	a, l
	ld	l, a
.neg_divisor_skip:

	push	bc

	ld	a, b
	rla
	jr	nc, .neg_dividend_skip
	xor	a, a
	sub	a, b
	ld	b, a
.neg_dividend_skip:

	call	__bdvrmu.hijack_l_dividend

	pop	bc
	ret


	extern	__bdvrmu.hijack_l_dividend
