	.assume	adl=1

	.section	.text
	.global	__stest
	.type	__stest, @function

__stest:
	; Z flag = ((HL & BC) == 0)
	; all other flags are destroyed
	push	de
	ld	e, a
	; test low 8 bits
	ld	a, l
	and	a, c
	jr	nz, .L.non_zero
	; test high 8 bits
	ld	a, h
	and	a, b
.L.non_zero:
	ld	a, e
	pop	de
	ret
