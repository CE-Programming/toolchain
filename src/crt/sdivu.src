	.assume	adl=1

	.section	.text
	.global	__sdivu
	.type	__sdivu, @function

.ifdef PREFER_CE_CRT

	.set	__sdivu, 0x00020C

.else

__sdivu:
; I: HL=dividend, BC=divisor
; O: uhl=HL/BC

	push	de

	call	__sdvrmu
	ex	de, hl

	pop	de
	ret

	.extern	__sdvrmu

.endif
