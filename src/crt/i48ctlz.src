; Performs 48-bit count leading zeros
;
; Returns:
; a = number of leading 0's in ude:uhl

	assume	adl=1

	section	.text
	public	__i48ctlz
__i48ctlz:
	push de
	push hl

	scf
	sbc a, a

.loop:
	adc hl, hl
	ex de, hl
	adc hl, hl
	ex de, hl
	inc a
	jr nc, .loop
	
	pop hl
	pop de
	ret