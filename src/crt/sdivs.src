	.assume	adl=1

	.section	.text
	.global	__sdivs
	.type	__sdivs, @function

.ifdef PREFER_OS_CRT

	.set	__sdivs, 0x000208

.else

__sdivs:
; I: HL=dividend, BC=divisor
; O: uhl=HL/BC

	push	de

	call	__sdvrms_abs
	ex	de, hl

	pop	de
	ret	p

	jp	__ineg

	.extern	__ineg
	.extern	__sdvrms_abs

.endif
