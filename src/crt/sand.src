	.assume	adl=1

	.section	.text
	.global	__sand
	.type	__sand, @function

.ifdef PREFER_CE_CRT

	.set	__sand, 0x000200

.else

__sand:
	push	af
	ld	a, h
	and	a, b
	ld	h, a
	ld	a, l
	and	a, c
	ld	l, a
	pop	af
	ret

.endif
