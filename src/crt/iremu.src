	.assume	adl=1

	.section	.text
	.global	__iremu
	.type	__iremu, @function

.ifdef PREFER_CE_CRT

	.set	__iremu, 0x000170

.else

__iremu:
; I: UHL=dividend, UBC=divisor
; O: uhl=UHL%UBC

	push	de

	call	__idvrmu

	pop	de
	ret

	.extern	__idvrmu

.endif
