	.assume	adl=1

	.section	.text
	.global	__sremu
	.type	__sremu, @function

.ifdef PREFER_OS_CRT

	.set	__sremu, 0x00023C

.else

__sremu:
; I: HL=dividend, BC=divisor
; O: uhl=HL%BC

	push	de

	call	__sdvrmu

	pop	de
	ret

	.extern	__sdvrmu

.endif
