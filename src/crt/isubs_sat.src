	.assume	adl=1

	.section	.text

	.global	__isubs_sat
	.type	__isubs_sat, @function

__isubs_sat:
	; signed saturated subtraction
	; HL = HL - BC
	or	a, a
	sbc	hl, bc
	ret	po
	ld	hl, $800000
	ret	p		; negative underflow
	; positive overflow
	inc	hl
	ret
