	.assume	adl=1

	.section	.text
	.global	__internal_bitrev_two_bytes
	.type	__internal_bitrev_two_bytes, @function

__internal_bitrev_two_bytes:
	rra
	adc	hl, hl
	rra
	adc	hl, hl
	rra
	adc	hl, hl
	rra
	adc	hl, hl
	rra
	adc	hl, hl
	rra
	adc	hl, hl
	rra
	adc	hl, hl
	rra
	adc	hl, hl
	rra
	ret
