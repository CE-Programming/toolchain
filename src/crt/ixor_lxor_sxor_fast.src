	assume	adl=1

	section	.text
	public	__lxor_fast
__lxor_fast:
	xor	a, e
	ld	e, a
	require	__ixor_fast

	section	.text
	public	__ixor_fast
__ixor_fast:
	push	hl
	scf
	sbc	hl, hl
	add	hl, sp
	push	bc
	ld	a, (hl)
	inc	hl
	ld	sp, hl
	inc	hl
	inc	hl
	xor	a, (hl)
	ld	(hl), a
	pop	hl
	require	__sxor_fast

	section	.text
	public	__sxor_fast
__sxor_fast:
	ld	a, h
	xor	a, b
	ld	h, a
	ld	a, l
	xor	a, c
	ld	l, a
	ret
