	.assume	adl=1

	.section	.text
	.global	__itest
	.type	__itest, @function

__itest:
	; Z flag = ((UHL & UBC) == 0)
	; all other flags are destroyed
	push	hl
	call	__iand
	add	hl, bc
	or	a, a
	sbc	hl, bc
	pop	hl
	ret

	.extern	__iand
