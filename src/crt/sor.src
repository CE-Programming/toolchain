	.assume	adl=1

	.section	.text
	.global	__sor
	.type	__sor, @function

.ifdef PREFER_CE_CRT

	.set	__sor, 0x000234

.else

__sor:
	push	af
	ld	a, h
	or	a, b
	ld	h, a
	ld	a, l
	or	a, c
	ld	l, a
	pop	af
	ret

.endif
