	.assume	adl=1

	.section	.text
	.global	__lsub
	.type	__lsub, @function

.ifdef PREFER_OS_CRT

	.set	__lsub, 0x0001F8

.else

__lsub:
	push	af
	or	a, a
	sbc	hl, bc
	cpl
	ccf
	adc	a, e
	ld	e, a
	pop	af
	ret

.endif
