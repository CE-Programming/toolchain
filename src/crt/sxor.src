	.assume	adl=1

	.section	.text
	.global	__sxor
	.type	__sxor, @function

.ifdef PREFER_OS_CRT

	.set	__sxor, 0x000268

.else

__sxor:
	push	af
	ld	a, h
	xor	a, b
	ld	h, a
	ld	a, l
	xor	a, c
	ld	l, a
	pop	af
	ret

.endif
