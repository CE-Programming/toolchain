	.assume	adl=1

	.section	.text

	.global	__setflag
	.type	__setflag, @function

.ifdef PREFER_CE_CRT

	.set	__setflag, 0x000218

.else

__setflag:
	ret	po
	push	af
	ex	(sp), hl
	ld	a, l
	xor	a, $80	; invert S flag
	ld	l, a
	ex	(sp), hl
	pop	af
	ret

.endif
