; Performs 48-bit signed comparison
;
; Arguments:
; ude:uhl = left-hand side
; uiy:ubc = right-hand side
;
; Returns:
; S and V flags updated according to comparison

	.assume	adl=1

	.section	.text
	.global	__i48cmps
	.type	__i48cmps, @function

__i48cmps:
; CC: 14 bytes | 15F + 9R + 6W + 1
	push	hl
	push	de
	or	a, a
	sbc	hl, bc	; compare hl to bc
	ex	de, hl	; hl = de
	lea	de, iy + 0	; de = iy
	sbc	hl, de	; compare de to iy
	pop	de
	pop	hl
	ret
