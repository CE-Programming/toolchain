	.assume	adl=1

	.section	.text

	.global	__iszerof
	.type	__iszerof, @function

; bool _iszerof(float)
__iszerof:
	xor	a, a
	pop	bc
	pop	hl
	adc	hl, hl
	ex	(sp), hl
	push	hl
	push	bc
	ret	nz	; non-zero mantissa
	ld	a, l
	adc	a, a
	sub a, 1
	sbc	a, a
	ret
