	.assume	adl=1

	.section	.text

	.global	__issubnormalf
	.type	__issubnormalf, @function

; bool _issubnormalf(float)
__issubnormalf:
	xor	a, a
	pop	bc
	pop	hl
	adc	hl, hl
	ex	(sp), hl
	push	hl
	push	bc
	ret	z	; zero mantissa is not subnormal
	adc	hl, hl
	sub	a, l
	sbc	a, a
	inc	a
	ret
