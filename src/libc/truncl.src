	assume	adl=1

	section	.text

	public	_truncl

; long double truncl(long double)
_truncl:
	ld	hl, 10
	add	hl, sp
	bit	7, (hl)	; signbit(x)
	jq	z, __truncl_c	; positive input
	; negative input
	res	7, (hl)	; x = fabsl(x)
	call	__truncl_c
	set 7, b	; copysignl(x, -1.0L)
	ret

	extern	__truncl_c
