	assume	adl=1

	section	.text
	public	_fabs, _fabsf, _fabsl

if PREFER_OS_LIBC

_fabsf := 0220F0h
_fabs := _fabsf

else

_fabs:
_fabsf:
	pop	bc, hl ,de
	push	de, hl, bc
	res	7, e
	ret

end if

_fabsl:
	pop	iy, hl ,de, bc
	push	bc, hl, de
	res	7, b
	jp	(iy)
