	assume	adl=1

	section	.text
	public	_strlen

if PREFER_OS_LIBC

_strlen := $0000D4

else

_strlen:
	pop	de
	ex	(sp),hl
	push	de
	push	hl
	pop	de
	xor	a,a
	ld	bc,0
	cpir
	scf
	sbc	hl,de
	ret

end if

