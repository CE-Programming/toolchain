	.assume	adl=1

	.section	.text

	.global	_sqrtl
	.type	_sqrtl, @function

_sqrtl:
	; flags handled by softfloat
	ld	hl, 10
	add	hl, sp
	ld	a, (hl)
	rlca
	inc	hl
	ld	(hl), a
	jp	___f64_sqrt

	.extern	___f64_sqrt
