include_rules
ezcflags += -isystem ../libc/include -isystem ../ce/include -isystem ../fileioc
: include/* |> !copy |> $(install)/%f $(install)/<install>
: *.c |> !ezcc |> {src}
: {src} *.src |> !copy |> $(install)/lib/libc/%b $(install)/<install>
