	.assume	adl=1

	.section	.text

	.global	__signbitl
	.type	__signbitl, @function

; bool _signbitl(long double)
__signbitl:
	ld	hl, 10
	add	hl, sp
	ld	a, (hl)
	rlca	; faster than rla \ sbc a, a
	ret
