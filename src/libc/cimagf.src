	.assume	adl=1

	.section	.text

	.global	_cimagf
	.type	_cimagf, @function
	.global	_cimag
	.type	_cimag, @function

; float cimagf(float _Complex)
_cimagf:
_cimag:
	ld	iy, 0
	add	iy, sp
	ld	sp, iy
	ld	hl, (iy + 7)
	ld	e, (iy + 10)
	ret
