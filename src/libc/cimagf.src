	.assume	adl=1

	.section	.text

	.global	_cimagf
	.type	_cimagf, @function
	.global	_cimag
	.type	_cimag, @function

; float cimagf(float _Complex)
_cimagf:
_cimag:
	ld	hl, 10
	add	hl, sp
	ld	e, (hl)
	dec	hl
	dec	hl
	dec	hl
	ld	hl, (hl)
	ret
