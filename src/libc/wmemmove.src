	assume	adl=1

	section	.text

	public	_wmemmove

; wchar_t *wmemmove(wchar_t *dest, const wchar_t *src, size_t count)
_wmemmove:
	ld	hl, 9
	add	hl, sp
	ld	iy, (hl)
	add	iy, iy		; count *= 2
	ld	(hl), iy
	jp	_memmove

	extern	_memmove
